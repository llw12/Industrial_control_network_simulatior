# 工业控制网络仿真平台（基于 OMNeT++）前后端接口规范 V1.1

作者：llw12  
更新时间：2025-11-09  
版本说明：在 V1.0 基础上完成“未确定点”确认与启动脚本更新，细化 NED/INI 生成策略（HIL 统一数组）、任务队列与状态机、端口冲突检测、Master/SlaveConfig 的 GUI 版接口、任意节点抓包配置、仿真结果可视化接口，以及完整的 OpenAPI/TypeScript/DDL 与生成伪代码。

---

## 0. 快速要点（与确认结果一致）
- Q10：客户端采用“统一数组”策略（client[numClients]）；HIL 通过 ini 覆盖 typename。
- Q3：IP 分配由 OMNeT++ 的 configurator 在 ini 中完成；后端不负责 IP 池分配。
- Q4：支持多客户端同时启用 HIL。
- Q5：抓包支持任意节点配置（不局限 server）。
- Q6：仅解析 SQLite 标量/向量；可在此基础上做常规聚合（min/max/avg/pXX）。
- Q7：引入任务队列/状态机，避免重复启动与便于资源调度。
- Q9：支持前端 GUI 编辑 Master/SlaveConfig（结构化），并提供版本管理。
- Q11：日志不做分级过滤（统一 INFO 流）。
- Q12：提供仿真结果可视化数据接口（聚合与 vector 分块）。
- Q13：同节点 localPort 不允许冲突。
- Q15：不支持暂停/恢复。

---

## 1. 架构与运行时

- 前端：Vue3 + Pinia + Ant Design Vue（或 Element Plus）+ G6（拓扑）
- 后端：Spring Boot + MyBatis/JPA，SQLite（仿真输出），MySQL/PostgreSQL（业务存储）
- 脚本：运行 inet；实时日志通过 WebSocket 推送
- 目录建议：
  ```
  /data/
    /projects/{projectCode}/
      /topology/         # .ned、MasterConfig.json、SlaveConfig.json、topology_v*.json
      /config/           # 历史 ini、节点配置快照
      /runs/{runId}/     # 单次运行：ini/ned拷贝、sqlite、pcap、日志
  ```

---

## 2. 统一规范

- API 前缀：/api/v1
- 返回 JSON：
  ```
  { "code": 0, "message": "成功", "data": {...}, "traceId": "可选" }
  ```
- 错误示例：
  ```
  { "code": 40001, "message": "项目名已存在", "data": null }
  ```
- 时间格式：ISO8601（UTC）`yyyy-MM-dd'T'HH:mm:ss'Z'`
- WebSocket：/ws/sim/log/{runId}（纯文本/JSON均可，推荐 JSON）
- 鉴权：当前不启用（Q2=否），保留扩展位

---

## 3. 数据模型（建议表）

- project：项目
- node：节点定义（含 paramsJson）
- topology：拓扑版本，含 graphJson、nedFilePath、Master/SlaveConfig 冗余
- config_file：Master/SlaveConfig 的版本库（GUI 编辑使用）
- simulation_run：仿真实例（状态机）
- simulation_result：解析后的指标（标量/向量或聚合数据缓存）

详见文件 ddl.sql。

---

## 4. 前端节点与参数

- 节点类型：OperatorStation、TsnSwitch、Server、Client（TSN 终端）
- Client 统一数组策略：
  - NED 始终 `client[numClients]: TsnDevice`
  - HIL 客户端在 ini 中覆盖 `*.client[i].app[0].typename = "ModbusSlaveHILApp"`

节点 params 关键字段：
- isHil: boolean
- eth: { bitrate, channelLength, delay(二选一，推荐 length) }
- apps: 按 typename 组织的键值对（字段映射表见第 7 节）

抓包配置（任意节点）：
- capture: { enable: boolean, moduleNamePatterns: string, pcapFile: string }

---

## 5. 接口总览（关键能力）

- 项目：CRUD、分页
- 节点：保存、批量、查询、删除、校验、端口冲突检测
- 配置文件（Master/SlaveConfig）：版本化 CRUD、校验
- 拓扑：保存、版本查询、预览 NED/INI、不落盘预览
- 仿真：预检查、启动（可入队）、停止、列表、详情、下载 INI/NED
- 日志：WebSocket 实时推流
- 结果：列表、导出、聚合统计、vector 分块
- HIL：客户端概览（索引、远端地址端口）

完整 OpenAPI 见文件 openapi.yaml；前端类型见 types.ts。

---

## 6. NED 生成策略（统一数组）

输入：
- graph（nodes/edges/meta）
- node 表（用于坐标与类型校验）

步骤：
1. network 名称：默认 ModbusTest1 或由 meta.networkName 指定（正规化）
2. parameters：`int numClients;`（来自 Client 节点计数）
3. submodules：
   - OperatorStation/Server/Switch：逐个生成 `id: 类型 { @display("p=x,y"); }`
   - Client：统一 `client[numClients]: TsnDevice { @display("p=652,329,r,80"); }`
4. connections：
   - 非客户端边：`{src}.ethg++ <--> EthernetLink <--> {dst}.ethg++;`
   - 客户端与交换机：用 for 循环生成到每台交换机的连接
5. 输出 .ned 文件到 /topology/

说明：
- 客户端数组元素坐标不在 NED 中细化（G6 画布负责视觉布局）

---

## 7. INI 生成策略（分段与覆盖）

段落组织：
- [General]：基础、默认 app 与链路设置
- [HardInLoop]：若存在 HIL 客户端，则添加 `scheduler-class = "inet::RealTimeScheduler"`，并对 HIL 索引覆盖 app typename 与远端参数

核心映射：
- output managers：SQLite
- 链路与以太：`*.*.eth[*].bitrate`、`*.*.ethg$o[*].channel.length/ber/per`
- TCP 参数：固定写死（按附录）
- 应用层：
  - 根据 node.apps 输出 `*.{nodeId}.app[i].字段 = 值`
  - Client 默认：`*.client[*].app[0].typename = "ModbusSlaveApp"`
  - HIL 覆盖：在 [HardInLoop] 段写 `*.client[idx].app[0].typename = "ModbusSlaveHILApp"` 与远端地址端口
- Master/SlaveConfig：只写引用路径
- 抓包（任意节点）：
  - 若 node.capture.enable：
    ```
    *.{nodeId}.numPcapRecorders = 1
    *.{nodeId}.pcapRecorder[0].moduleNamePatterns = "{patterns}"
    *.{nodeId}.pcapRecorder[0].pcapFile = "results/{nodeId}.pcap"
    ```

字段映射表（示例）：
- OperatorStationApp: [localPort, connectAddress, connectPort, startTime, interval, reconnectInterval]
- OperatorStationApp2: [localPort, connectAddress, connectPort, modbusRequest, sendTime, seed, reconnectInterval]
- ModbusMasterApp: [localPort, connectport, numConnect, configFile, readInterval]
- ModbusTcpServerApp: [localPort]
- TransitApp: [localPort]
- ModbusSlaveApp: [localPort, slavesConfigPath]
- ModbusSlaveHILApp: [localPort, remoteAddress, remotePort, slavesConfigPath]

校验要点：
- 时间单位：如无单位默认 s
- 端口：1..65535
- HIL：remoteAddress/remotePort 必填
- Master/SlaveConfig：引用文件需存在（或在拓扑版本中冗余）
- 抓包：pcap 路径写在 run 目录 results/ 下

---

## 8. 启动/停止与日志

启动：
- POST /simulations/start
- 生成 runId，拷贝 .ned/.ini 到 runs/{runId}/
- 调用脚本（见第 16 节），实时读取 stdout 推送到 /ws/sim/log/{runId}
- 结束解析 SQLite，落库 simulation_result

停止：
- POST /simulations/{runId}/stop
- 执行 stop 脚本或 kill 运行进程（需维护 PID 文件或以子进程句柄）

日志 WebSocket 消息：
```
{"runId":"run_x","timestamp":1731150400000,"level":"INFO","content":"..."}
```
结束标志：
```
{"runId":"run_x","level":"END","content":"Simulation finished status=2"}
```

---

## 9. 任务队列与状态机

状态（simulation_run.status）：
- 0 STARTING，1 RUNNING，2 FINISHED，3 FAILED，4 STOPPED

队列（可启用）：
- /simulations/start?queued=true 进入 WAITING
- 调度到资源可用时转 STARTING

并发策略：
- Q1=否并发限制：可允许多 run 并发，但建议加资源阈值与队列（Q7=是）

---

## 10. 结果解析与可视化

解析：
- 仅解析 SQLite 标量（scalars）与向量（vectors）（Q6）
- 存储到 simulation_result（向量可仅存引用与部分抽样/统计）

可视化接口：
- /simulation-results（分页筛选）
- /simulation-results/aggregate（min/max/avg/p95/p99 等，基于向量数据在线计算）
- /simulation-results/vector（offset/limit 分块）
- /simulation-results/export（csv/json）

建议图表：
- 时序曲线（vector）
- 直方图/箱线图（延迟分布）
- 拓扑高亮（按模块指标着色）

---

## 11. 校验与错误码

关键校验：
- 端口冲突（同节点 localPort 不可重复）→ 40009
- HIL 缺字段 → 40006
- 配置 JSON 合法性（Master/Slave）→ 40007/40008
- 拓扑必要性（如必须包含 server、至少 1 客户端）→ 40003/40010
- 启动参数缺失 → 40004
- 脚本失败 → 50001
- INI/NED 生成失败 → 50003/50004
- SQLite 解析异常 → 50002

完整错误码见文末表格与 openapi.yaml。

---

## 12. 前端交互流程

1) 新建项目 → 进入拓扑编辑  
2) 拖拽节点/连线（G6） → 双击节点打开配置抽屉（isHil、apps、capture）  
3) 保存拓扑（生成 NED、保存 Master/SlaveConfig）  
4) 预览 INI（不落盘） → 启动前预检查（端口、HIL、配置）  
5) 启动仿真 → 打开日志弹窗（WebSocket）  
6) 完成后进入结果页 → 查询/筛选/聚合/分块曲线 → 导出  
7) 若需变更 → 新版拓扑/重跑

---

## 13. 安全与运维

- 禁止脚本注入：参数白名单（-f, -n, -u, -Cmdenv, -c）
- 文件路径规范化，防目录穿越
- 运行用户权限最小化（无 root）
- 清理策略：定期清理 runs 目录大文件（pcap/sqlite）或提供一键清理接口（管理员）

---

## 14. 性能

- NED/INI 生成缓存（输入哈希）
- Vector 分块与虚拟滚动
- SQLite 打开为只读，必要时建索引
- 日志行缓冲（stdbuf -oL）降延迟

---

## 15. 开发落地建议

- 先打通主链路（项目→节点→拓扑→预览→启动→日志→解析→结果）
- 再补 GUI 配置编辑、端口冲突、队列/状态机
- ECharts/AntV G2 构建可视化（折线、直方、箱线）

---

## 16. 启动/停止脚本（引用确认版）

run_sim.sh：
```
#!/bin/bash
set -e
INI_FILE=$1
NED_FILE=$2
RUN_DIR=$3

# 设置 OMNeT++ 环境
cd ~/omnetpp-6.1/
source setenv
# 设置 INET 环境
cd ~/omnetpp-6.1/WorkSpace/inet4.5
source setenv

cd "$RUN_DIR"
echo "[INFO] Using INI: $INI_FILE"
echo "[INFO] Using NED: $NED_FILE"

# 启动仿真（标准 C++ 环境）
inet -n . -f "$INI_FILE" -u -Cmdenv -c General
```

stop_sim.sh：
```
#!/bin/bash
PID_FILE=$1
if [ -f "$PID_FILE" ]; then
  kill $(cat "$PID_FILE")
  echo "[INFO] Simulation stopped."
else
  echo "[WARN] PID file not found."
fi
```

注：建议在后端记录 inet 进程 PID 到 RUN_DIR/pid，stop 时使用。

---

## 17. 错误码建议表

| code | message | 说明 |
|------|---------|------|
| 40001 | 项目名已存在 | 新建项目冲突 |
| 40002 | 节点类型不支持 | 前端传入错误 |
| 40003 | 拓扑缺少必需节点 | 例如缺少 server |
| 40004 | 启动参数缺失/非法 | simTimeLimit 等 |
| 40005 | 仿真运行中禁止重复启动 | 可用队列 |
| 40006 | HIL 客户端配置缺失 | remoteAddress/remotePort |
| 40007 | MasterConfig 内容非法 | GUI 校验失败 |
| 40008 | SlaveConfig 内容非法 | GUI 校验失败 |
| 40009 | 端口冲突 | 同节点 localPort 冲突 |
| 40010 | 无有效客户端 | Client 数量为 0 |
| 40401 | 项目不存在 |  |
| 40402 | 节点不存在 |  |
| 40403 | 拓扑版本/文件不存在 |  |
| 40404 | 仿真实例不存在 |  |
| 50001 | 脚本执行失败 |  |
| 50002 | SQLite 解析异常 |  |
| 50003 | INI 生成失败 | 模板/数据异常 |
| 50004 | NED 生成失败 | 拓扑转换异常 |
| 50005 | 任务队列调度失败 |  |

---

## 18. 参考与附录

- NED 与 INI 的示例格式已融合到生成策略，不再重复贴原文。
- 完整 OpenAPI、前端类型、DDL、生成伪代码见附加文件。

---